// import { Page } from 'playwright';  


// export const cursorElements = await page.evaluate(() => {
//     const allElements = [...document.querySelectorAll('*')];
//     const clickableElements = allElements.filter(el => getComputedStyle(el).cursor === 'pointer');
//     return clickableElements.map((el, index) => {
//         if (el.id) return `#${el.id}`;
//         return `css=${el.tagName.toLowerCase()}:nth-of-type(${index + 1})`;
//     });
// });
